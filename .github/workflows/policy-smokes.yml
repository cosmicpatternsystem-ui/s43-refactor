name: Policy Smoke Tests

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  policy-smokes:
    name: policy-smokes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run policy smoke checks
        run: bash run_safe.sh policy
